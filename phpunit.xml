<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         stopOnFailure="false">

    <testsuites>
        <testsuite name="base">
            <directory>tests/Base</directory>
        </testsuite>
        <testsuite name="working">
            <directory>tests/Working</directory>
        </testsuite>
    </testsuites>

    <!--
    Heavy, environment-specific tests live under tests/Container and are tagged
    with these groups. They are EXCLUDED from the default run (composer test) and
    executed only inside containers via tests/run-container.sh or CI (the
    container phpunit group).
    -->
    <groups>
        <exclude>
            <group>container</group>
            <group>swoole</group>
            <group>fpm</group>
            <group>leak</group>
            <group>timing</group>
        </exclude>
    </groups>

    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
    </source>

</phpunit>
